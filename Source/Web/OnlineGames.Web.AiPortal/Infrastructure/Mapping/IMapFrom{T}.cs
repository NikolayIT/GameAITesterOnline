namespace OnlineGames.Web.AiPortal.Infrastructure.Mapping
{
    // ReSharper disable once UnusedTypeParameter (used in the reflection code when creating Automapper mappings)
    public interface IMapFrom<T>
    {
    }
}
